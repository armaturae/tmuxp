- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.tmux/tmuxp.projects.yaml: tmuxp.projects.yaml
    ~/.tmux/tmuxp.home.yaml: tmuxp.home.yaml
    ~/.tmux/tmuxp.monitoring.yaml: tmuxp.monitoring.yaml

- create:
    - ~/.tmux/
